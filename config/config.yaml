# MStorm Asset Forge Configuration File

# Storage and Output settings
storage:
  output_dir: "outputs"
  use_registry: true

# Blender settings
blender:
  headless_rendering: true
  gpu_rendering: false
  timeout: 300
